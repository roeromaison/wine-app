# 本番ビルド(npm run build)のときだけ読まれる設定。
#
# 公開版は「画面は静的ホスティング・APIは別サーバー」に分けている。
# 画面から見るとAPIは別オリジンになるので、絶対URLで指定する必要がある。
#
# APIのURLを変えたときは、ここを書き換えてGitHubにpushすれば
# 静的ホスティング側が自動で作り直す。

VITE_APP_MODE=public
VITE_API_BASE=https://wine-app-hp67.onrender.com
